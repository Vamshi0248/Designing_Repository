// Dummy code for feature/navigation branch
function createNavigation() {
  console.log("Creating navigation components...");
  const navItems = ['Home', 'About', 'Services', 'Contact', 'Blog'];
  navItems.forEach(item => {
    console.log(`Adding navigation item: ${item}`);
  });
}

createNavigation();
